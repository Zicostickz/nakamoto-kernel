[project]
name = "nakamoto-kernel"
authors = ["Nakamoto Protocol Team"]
description = "Decentralized infrastructure for secure, user-controlled data networks"
telemetry = false
requirements = []
[contracts.core-protocol]
path = "contracts/core-protocol.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
